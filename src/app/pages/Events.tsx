import React from 'react';
import styled from 'styled-components';

const Events: PageComponent = () => {
  return <Root>Hello, World. Events</Root>;
};

const Root = styled.div`
  font-size: 20px;
`;

export default Events;
