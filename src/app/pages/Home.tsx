import React from 'react';
import styled from 'styled-components';

const Home: PageComponent = () => {
  return <Root>Hello, World. Home</Root>;
};

const Root = styled.div`
  font-size: 20px;
`;

export default Home;
